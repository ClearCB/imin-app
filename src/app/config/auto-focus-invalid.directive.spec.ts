import { AutoFocusInvalidDirective } from './auto-focus-invalid.directive';

describe('AutoFocusInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFocusInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
